# Dependencies
node_modules/

# Build
dist/
__pycache__/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# m-control config (may hold secrets). Global config lives in ~/.m-control/,
# outside the repo; this catches a project config created at the repo root.
.m-control/
.env

# Package managers other than the pinned Yarn (see packageManager)
package-lock.json
pnpm-lock.yaml

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
*.swp
*.swo
*~

# Cursor cache (but keep committed project rules)
.cursor/*
!.cursor/rules/

# Claude Code personal files (shared settings live in .claude/settings.json)
.claude/settings.local.json
CLAUDE.local.md
